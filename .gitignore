# macOS
**/.DS_Store

# Local Claude Code settings — never commit
.claude/settings.local.json

# Credentials — never commit
.super8-studio.env

# CI-generated — injected at pack time, not committed
skills/_super8-studio-api-shared/RELEASE
